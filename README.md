# Personal site (React + Vite)

- **Development:** `npm run dev`
- **Build:** `npm run build` (output in `dist/`)

To deploy to GitHub Pages from the built files: copy the contents of `dist/` to the root (or use a GitHub Action to build and deploy).
